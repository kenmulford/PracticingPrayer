• Accessibility: at large text sizes, the Home dashboard now reflows to a single column so the summary counts stay fully visible instead of getting cut off.
• Reliability improvements behind the scenes.
